# What are we making?
- Minimal HTML/JS site
-This has the following button which maps to the solidity smart contract
    -Connect
    -Buy coffee
        -Button to buy coffee
        -Call a function on a smart contract
        -Have a test blockchain that we can call?
    -Get balance
    -Withdraw